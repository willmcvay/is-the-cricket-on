export default {
  networkStatus: 200,
  loading: false,
  variables: {},
  fetchMore: jest.fn(),
  refetch: jest.fn(),
  startPolling: jest.fn(),
  stopPolling: jest.fn(),
  subscribeToMore: jest.fn(),
  updateQuery: jest.fn()
}
