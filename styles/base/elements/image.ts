import styled from '../../styled-components'
import { layoutDouble } from '../variables/layout'

const StyledImage = styled.img`
  object-fit: cover;
  width: ${layoutDouble};
  height: ${layoutDouble};
  border-radius: 50%;
`

export default StyledImage
