query matchDetails($matchid: String!, $seriesid: String!) {
  matchDetails(matchid: $matchid, seriesid: $seriesid) {
    matchDetail {
      matchSummary {
        id
        matchTypeId
        name
        status
      }
    }
  }
}